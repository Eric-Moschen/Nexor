services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-nexor}
      POSTGRES_USER: ${POSTGRES_USER:-nexor}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-nexor_dev_password}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-nexor} -d ${POSTGRES_DB:-nexor}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      args:
        REQUIREMENTS_FILE: local.txt
    command: python manage.py runserver 0.0.0.0:8000
    env_file:
      - ${NEXOR_ENV_FILE:-./.env}
    volumes:
      - ./backend:/app
      - ./logs:/logs
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8000/api/v1/health/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5

  celery:
    build:
      context: ./backend
      args:
        REQUIREMENTS_FILE: local.txt
    command: celery -A config worker -l info
    env_file:
      - ${NEXOR_ENV_FILE:-./.env}
    volumes:
      - ./backend:/app
      - ./logs:/logs
    depends_on:
      - backend
      - redis

  celery-beat:
    build:
      context: ./backend
      args:
        REQUIREMENTS_FILE: local.txt
    command: celery -A config beat -l info
    env_file:
      - ${NEXOR_ENV_FILE:-./.env}
    volumes:
      - ./backend:/app
      - ./logs:/logs
    depends_on:
      - backend
      - redis

  frontend:
    build:
      context: ./frontend
      target: development
    environment:
      VITE_API_BASE_URL: http://localhost:8000/api/v1
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    depends_on:
      - backend

volumes:
  postgres_data:
  redis_data:
